using DatabaseBuilder.Builders.MTG;
using static DatabaseBuilder.Builders.MTG.Helpers;

namespace DatabaseBuilder.Builders.MTG
{
	internal static class Table
	{
		public static void BuildTables(string projectDirectory, string connectionString)
		{
			//List<TableReference> refs = new List<TableReference>();
			//string[] files = Directory.GetFiles(projectDirectory + "\\Table");
			//foreach (var file in files)
			//{
   //             string f = File.ReadAllText(file);
			//	refs.AddRange(GetReferences(file.Split('\\').Last().Split('.').First(), f));
			//}
		}
	}
}
